<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyNotes.Views.AllNotesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyNotes.Views"
    xmlns:models="using:MyNotes.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="NoteItemTemplate" 
                  x:DataType="models:Note">
            <ItemContainer Width="200" Margin="4" CornerRadius="{StaticResource OverlayCornerRadius}">
                <ItemContainer.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="{Binding DataContext.Menu_Edit_Text, ElementName=RootGrid, Mode=OneWay}" Icon="Edit" Click="Edit_Click" CommandParameter="{x:Bind Filename}"/>
                        <MenuFlyoutItem Text="{Binding DataContext.Menu_Delete_Text, ElementName=RootGrid, Mode=OneWay}" Icon="Delete" Click="Delete_Click" CommandParameter="{x:Bind Filename}"/>
                    </MenuFlyout>
                </ItemContainer.ContextFlyout>
                    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderThickness="1" 
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        CornerRadius="{StaticResource OverlayCornerRadius}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="120"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="NoteText" Text="{x:Bind Text}" Margin="12,8"
                           TextWrapping="Wrap"
                           TextTrimming="WordEllipsis"/>
                    <Border Grid.Row="1" Padding="8,6,0,6"
                        Background="{ThemeResource SubtleFillColorSecondaryBrush}">
                        <TextBlock Text="{x:Bind Date}"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </Border>
                </Grid>
            </ItemContainer>
        </DataTemplate>
        <SolidColorBrush x:Key="CommandBarBackgroundOpen"
        Color="Transparent" />
        <Thickness x:Key="CommandBarBorderThicknessOpen">0</Thickness>
        <!-- Remove default GridView item hover/pressed/selected background -->
        <Style x:Key="GridViewItemNoHoverStyle" TargetType="GridViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewItem">
                        <ListViewItemPresenter
                            Content="{TemplateBinding Content}"
                            ContentTransitions="{TemplateBinding ContentTransitions}"
                            Padding="{TemplateBinding Padding}"
                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                            PointerOverBackground="Transparent"
                            PressedBackground="Transparent"
                            SelectedBackground="Transparent"
                            SelectedPointerOverBackground="Transparent"
                            SelectedPressedBackground="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid x:Name="RootGrid" Padding="16" RowSpacing="8" DataContext="{Binding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar DefaultLabelPosition="Right">
            <AppBarButton Icon="Add" Label="{Binding AllNotes_NewNote_Label, Mode=OneWay}" Click="NewNoteButton_Click"/>
            <CommandBar.Content>
                <TextBlock x:Name="AllNotesTitle" Text="{Binding AllNotes_Title, Mode=OneWay}" Margin="16,8" 
                   Style="{ThemeResource SubtitleTextBlockStyle}"/>
            </CommandBar.Content>
            <CommandBar.SecondaryCommands>
                <AppBarButton Icon="Setting" Label="{Binding AllNotes_Settings_Label, Mode=OneWay}" Click="SettingsButton_Click"/>
            </CommandBar.SecondaryCommands>
        </CommandBar>

        <GridView x:Name="NotesView" ItemsSource="{x:Bind notesModel.Notes}" 
            Grid.Row="1" Padding="16" ItemTemplate="{StaticResource NoteItemTemplate}"
            ItemContainerStyle="{StaticResource GridViewItemNoHoverStyle}"
            SelectionMode="None"
            IsItemClickEnabled="True"
            ItemClick="NotesView_ItemClick"
            CanReorderItems="True"
            AllowDrop="True">
        </GridView>
    </Grid>
</Page>
