<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyNotes.Views.EditNote"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyNotes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="16"/>
        </Grid.ColumnDefinitions>

        <Grid Padding="0" RowSpacing="4" Grid.Row="0" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="BackButton"
                Click="BackButton_Click"
                Style="{StaticResource TitleBarBackButtonStyle}"
                IsEnabled="{x:Bind Frame.CanGoBack, Mode=OneWay}" 
                ToolTipService.ToolTip="Назад"
                Grid.Row="0" Grid.Column="0"/>
            <TextBlock Text="{x:Bind noteModel.Date.ToString()}" Margin="16,8" 
                Style="{ThemeResource SubtitleTextBlockStyle}" Grid.Row="0" Grid.Column="1"/>
            <Button Style="{StaticResource SubtleButtonStyle}" Name="ButtonDelete" Click="DeleteButton_Click" Grid.Row="0" Grid.Column="2">
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <SymbolIcon Symbol="Delete" />
                    <TextBlock Text="Удалить"/>
                </StackPanel>
            </Button>
        </Grid>
        
        <TextBox x:Name="NoteEditor"
            Text="{x:Bind noteModel.Text, Mode=TwoWay}"
            AcceptsReturn="True"
            TextWrapping="Wrap"
            PlaceholderText="Введите текст заметки..."
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            Width="Auto"
            Grid.Row="1" Grid.Column="1"/>
    </Grid>
</Page>
