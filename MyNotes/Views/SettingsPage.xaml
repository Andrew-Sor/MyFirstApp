<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyNotes.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyNotes.Views"
    xmlns:localizer="using:MyNotes.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid Padding="16" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <CommandBar Background="Transparent" IsOpen="False" HorizontalContentAlignment="Left" DefaultLabelPosition="Right" Grid.Row="0" Grid.Column="0">
            <CommandBar.Content>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                    <Button x:Name="BackButton"
                x:Uid="Settings_BackButton"
                Click="BackButton_Click"
                Style="{StaticResource TitleBarBackButtonStyle}"
                IsEnabled="{x:Bind Frame.CanGoBack, Mode=OneWay}" 
                ToolTipService.ToolTip="Назад"/>
                    <TextBlock x:Name="TitleTextBlock" x:Uid="Settings_TitleText" Text="{Binding SettingsTitle, Mode=OneWay}" Margin="16,8" 
                Style="{ThemeResource SubtitleTextBlockStyle}"/>
                </StackPanel>
            </CommandBar.Content>
        </CommandBar>

        <ScrollViewer Grid.Row="1" Grid.Column="0">
            <StackPanel Orientation="Vertical" Spacing="4" Width="auto" Padding="18, 0">
                <TextBlock x:Name="AppearanceLabel" x:Uid="Settings_AppearanceLabel" Text="{Binding AppearanceLabel, Mode=OneWay}" Margin="4"/>
                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    VerticalAlignment="Top"
                    Width="auto"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Left">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal" Spacing="16" Padding="16">
                            <FontIcon Glyph="&#xE790;" />
                                <StackPanel Orientation="Vertical" Spacing="0">
                                <TextBlock x:Name="ThemeHeaderTitle" x:Uid="Theme_HeaderTitle" Text="{Binding ThemeHeaderTitle, Mode=OneWay}" FontSize="14"/>
                                <TextBlock x:Name="ThemeHeaderSubtitle" x:Uid="Theme_HeaderSubtitle" Text="{Binding ThemeHeaderSubtitle, Mode=OneWay}" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                    </Expander.Header>
                    <Expander.Content>
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <RadioButtons x:Uid="ThemeRadioButtons" Header="{Binding ThemeHeaderTitle, Mode=OneWay}" x:Name="ThemeRadioButtons" SelectionChanged="ThemeRadioButtons_SelectionChanged">
                                <RadioButton x:Uid="Theme_Light"
                        Content="{Binding ThemeLight, Mode=OneWay}"
                        Tag="Light" />
                                <RadioButton x:Uid="Theme_Dark"
                        Content="{Binding ThemeDark, Mode=OneWay}"
                        Tag="Dark" />
                                <RadioButton x:Uid="Theme_Default"
                        Content="{Binding ThemeDefault, Mode=OneWay}"
                        Tag="Default" />
                            </RadioButtons>
                        </StackPanel>
                    </Expander.Content>
                </Expander>
                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    VerticalAlignment="Top"
                    Width="auto"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Left">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal" Spacing="16" Padding="16">
                            <SymbolIcon Symbol="Font"/>
                                <StackPanel Orientation="Vertical" Spacing="0">
                                <TextBlock x:Name="TextHeaderTitle" x:Uid="Text_HeaderTitle" Text="{Binding TextHeaderTitle, Mode=OneWay}" FontSize="14"/>
                                <TextBlock x:Name="TextHeaderSubtitle" x:Uid="Text_HeaderSubtitle" Text="{Binding TextHeaderSubtitle, Mode=OneWay}" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                    </Expander.Header>
                    <Expander.Content>
                        <StackPanel Orientation="Vertical" Spacing="4">
                            <ComboBox x:Name="FontComboBox" x:Uid="FontComboBox" IsEditable="True" ItemsSource="" Header="{Binding FontComboBoxHeader, Mode=OneWay}" Width="300"/>
                            <ComboBox x:Name="FontSizeComboBox" x:Uid="FontSizeComboBox" IsEditable="True" ItemsSource="" Header="{Binding FontSizeComboBoxHeader, Mode=OneWay}" Width="300" Margin="0, 20, 0, 0"/>
                        </StackPanel>
                    </Expander.Content>
                </Expander>

                <TextBlock x:Name="LanguageLabel" x:Uid="Language_Label" Text="{Binding LanguageLabel, Mode=OneWay}" Margin="4"/>
                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    VerticalAlignment="Top"
                    Width="auto"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Left">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal" Spacing="16" Padding="16">
                            <FontIcon Glyph="&#xF2B7;" />
                                <StackPanel Orientation="Vertical" Spacing="0">
                                <TextBlock x:Name="LanguageHeaderTitle" x:Uid="Language_HeaderTitle" Text="{Binding LanguageHeaderTitle, Mode=OneWay}" FontSize="14"/>
                                <TextBlock x:Name="LanguageHeaderSubtitle" x:Uid="Language_HeaderSubtitle" Text="{Binding LanguageHeaderSubtitle, Mode=OneWay}" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                    </Expander.Header>
                    <Expander.Content>
                        <StackPanel Orientation="Vertical" Spacing="4">
                            <ComboBox x:Uid="LanguageComboBox" x:Name="LanguageComboBox" Header="Выберите язык:" Width="300" SelectionChanged="LanguageComboBox_SelectionChanged"/>
                        </StackPanel>
                    </Expander.Content>
                </Expander>

                <TextBlock Text="Сведения" Margin="4"/>
                <Expander
                    IsExpanded="False"
                    ExpandDirection="Down"
                    VerticalAlignment="Top"
                    Width="auto"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Left">
                    <Expander.Header>
                        <Grid Padding="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Spacing="16" Grid.Column="0">
                                <FontIcon Glyph="&#xF4AA;" />
                                <StackPanel Orientation="Vertical" Spacing="0">
                                    <TextBlock x:Name="InfoTitleTextBlock" x:Uid="Info_Title" Text="{Binding InfoTitle, Mode=OneWay}" FontSize="14"/>
                                    <TextBlock x:Name="InfoCopyrightTextBlock" x:Uid="Info_Copyright" Text="{Binding InfoCopyright, Mode=OneWay}" FontSize="12"/>
                                </StackPanel>
                            </StackPanel>
                            <TextBlock x:Name="VersionTextBlock" x:Uid="VersionText" IsTextSelectionEnabled="True" Text="{Binding VersionText, Mode=OneWay}" VerticalAlignment="Center" Grid.Column="1" HorizontalAlignment="Right"/>
                        </Grid>
                    </Expander.Header>
                    <Expander.Content>
                        <StackPanel Orientation="Vertical" Spacing="4">
                            <HyperlinkButton x:Name="UpdateButton" x:Uid="UpdateButton" Content="{Binding UpdateButton, Mode=OneWay}" NavigateUri="https://github.com/Andrew-Sor/MyNotes/releases" />
                            <HyperlinkButton x:Name="RepoButton" x:Uid="RepoButton" Content="{Binding RepoButton, Mode=OneWay}" NavigateUri="https://github.com/Andrew-Sor/MyNotes" />
                            <HyperlinkButton x:Name="IssueButton" x:Uid="IssueButton" Content="{Binding IssueButton, Mode=OneWay}" NavigateUri="https://github.com/Andrew-Sor/MyNotes/issues/new" />
                            <HyperlinkButton x:Name="SupportButton" x:Uid="SupportButton" Content="{Binding SupportButton, Mode=OneWay}" NavigateUri="" />
                            <HyperlinkButton x:Name="DeveloperButton" x:Uid="DeveloperButton" Content="{Binding DeveloperButton, Mode=OneWay}" NavigateUri="https://github.com/Andrew-Sor" />
                        </StackPanel>
                    </Expander.Content>
                </Expander>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
